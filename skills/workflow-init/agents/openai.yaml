interface:
  display_name: "Workflow Init"
  short_description: "Set up project checks and agent conventions"
  default_prompt: "Use $workflow-init to configure this repository's checks, local planning conventions, domain docs, and safe publication policy."
