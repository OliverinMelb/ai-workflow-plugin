interface:
  display_name: "Codex Workflow"
  short_description: "Align, specify, execute, and verify coding tasks safely"
  default_prompt: "Use $codex-workflow to align decisions, create the local spec and tickets when needed, then implement and verify this coding task."
